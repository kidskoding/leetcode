#ifndef MAIN_H
#define MAIN_H

#include <string>
#include <vector>

std::vector<int> twoSum(std::vector<int>& nums, int target);

std::string reverseVowels(std::string s);
bool isVowel(char c);

#endif