"""
Lowest Common Ancestor of a Binary Tree

Given the root of a binary tree and two nodes p and q, return their lowest 
common ancestor (LCA).

The lowest common ancestor is defined as the lowest node in the tree that has 
both p and q as descendants (where a node can be a descendant of itself).

Each node in the tree is represented by a TreeNode object:
    class TreeNode:
        def __init__(self, val=0, left=None, right=None):
            self.val = val
            self.left = left
            self.right = right

Example 1:
    Input:
        root = [3,5,1,6,2,0,8,null,null,7,4]
        p = 5
        q = 1
    Output: 3

Example 2:
    Input:
        root = [3,5,1,6,2,0,8,null,null,7,4]
        p = 5
        q = 4
    Output: 5

Example 3:
    Input:
        root = [1,2]
        p = 1
        q = 2
    Output: 1

Constraints:
    2 <= number of nodes <= 10^5
    -10^9 <= Node.val <= 10^9
    All Node values are unique
    p != q
    Both p and q exist in the tree
"""