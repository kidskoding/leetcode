"""
Is Graph Bipartite?

There is an undirected graph with n nodes. You are given a 2D array graph
where graph[u] is an array of nodes that node u is adjacent to.

A graph is bipartite if you can split its nodes into two independent sets A 
and B such that every edge connects a node in A to a node in B.

Return true if the graph is bipartite, false otherwise.

Example 1:
    Input: graph = [[1,2,3],[0,2],[0,1,3],[0,2]]
    Output: false

Example 2:
    Input: graph = [[1,3],[0,2],[1,3],[0,2]]
    Output: true

Constraints:
    graph.length == n
    1 <= n <= 100
    0 <= graph[u].length < n
    graph[u] does not contain u
    All values of graph[u] are unique
"""